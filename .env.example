# Replace the API-key placeholder. Add listener secrets after `stripe listen` starts.
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=
STRIPE_THIN_WEBHOOK_SECRET=

# The app builds fixed local callback URLs from this origin.
APP_BASE_URL=http://127.0.0.1:8000
STATE_DIR=.local
